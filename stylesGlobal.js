import { StyleSheet } from 'react-native'

export default (backgroundColor='#A050BE') => StyleSheet.create({
    fill: {
        flex: 1,
        backgroundColor: backgroundColor,
    }
})

export const colors = {
    roxo: '#A050BE',
    laranja: '#FABE50',
    escuro: '#555555',
    claro: '#fff',
    cinza: '#C7C7C7',
}